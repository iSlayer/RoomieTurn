<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.roomieturn"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <!-- Allow to connect with Internet and to know the current network state -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Modifiedtoolbar" >
        <activity
            android:name="com.example.roomieturn.NewChore"
            android:label="@string/title_activity_new_chore" >
        </activity>
        <activity
            android:name="com.example.roomieturn.RecentTasks"
            android:label="@string/title_activity_recent_tasks" >
        </activity>
        <activity
            android:name="com.example.roomieturn.LoginActivity"
            android:label="@string/title_activity_login"
            android:launchMode="singleInstance"
            android:theme="@android:style/Theme.Light.NoTitleBar"
            android:windowSoftInputMode="adjustResize|stateVisible" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.roomieturn.Register"
            android:label="@string/title_activity_register"
            android:launchMode="singleInstance"
            android:theme="@android:style/Theme.Light.NoTitleBar" >
        </activity>
        <activity
            android:name="com.example.roomieturn.PasswordReset"
            android:label="@string/title_activity_password_reset"
            android:launchMode="singleInstance"
            android:theme="@android:style/Theme.Light.NoTitleBar" >
        </activity>
        <activity
            android:name="com.example.roomieturn.ChangePassword"
            android:label="@string/title_activity_change_password"
            android:launchMode="singleInstance"
            android:theme="@android:style/Theme.Light.NoTitleBar" >
        </activity>
        <activity
            android:name="com.example.roomieturn.HouseMenu"
            android:label="@string/title_activity_house_menu"
            android:launchMode="singleInstance"
            android:theme="@android:style/Theme.Light.NoTitleBar" >
        </activity>
    </application>

</manifest>